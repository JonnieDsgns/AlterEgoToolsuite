<UserControl x:Class="AlterEgoEditor.Views.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:Core.Models;assembly=Core"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             Background="#FF161616">
    
    <Grid Name="MainViewArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="44"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Lower Menu Bar -->
        <Grid Name="LowerMenuBar" Grid.Row="0">
            <DockPanel>
                <StackPanel Orientation="Horizontal" Height="44" VerticalAlignment="Top" Background="#FF282828">
                    <Button x:Name="btnNewProject" FontFamily="Segoe Fluent Icons" Content="&#xE710;" FontSize="16" Width="44" Height="44" Background="Transparent" BorderBrush="Transparent" Foreground="White"/>
                    <Button x:Name="btnOpenProject" FontFamily="Segoe Fluent Icons" Content="&#xE838;" FontSize="16" Width="44" Height="44" Background="Transparent" BorderBrush="Transparent" Foreground="White"/>
                    <Button x:Name="btnSave" FontFamily="Segoe Fluent Icons" Content="&#xE74E;" FontSize="16" Width="44" Height="44" Background="Transparent" BorderBrush="Transparent" Foreground="White"/>
                    <Separator Margin="5, 0, 5, 0" Height="30"/>
                    <Button x:Name="btnImport" FontFamily="Segoe Fluent Icons" Content="&#xE896;" FontSize="16" Width="44" Height="44" Background="Transparent" BorderBrush="Transparent" Foreground="White"/>
                    <Button x:Name="btnExport" FontFamily="Segoe Fluent Icons" Content="&#xE898;" FontSize="16" Width="44" Height="44" Background="Transparent" BorderBrush="Transparent" Foreground="White"/>
                </StackPanel>
            </DockPanel>
        </Grid>

        <!-- Main Content Viewer -->
        <Grid Name="ContentViewer" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="350"/>
                <ColumnDefinition Width="2"/>
                <ColumnDefinition Width="2*" MinWidth="400"/>
            </Grid.ColumnDefinitions>
            <Grid Name="GameFileViewer" Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" MinHeight="300"/>
                    <RowDefinition Height="2"/>
                    <RowDefinition Height="*" MinHeight="150"/>
                </Grid.RowDefinitions>
                <Grid Name="OutlinerTreeView" Grid.Row="0">
                    <TreeView ItemsSource="{Binding RootNodes}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectedItemChanged">
                                <i:InvokeCommandAction Command="{Binding SelectNodeCommand}" 
                                   CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=TreeView}}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate DataType="{x:Type models:FileNode}" ItemsSource="{Binding Children}">

                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding ResourceType}" Margin="0,0,5,0" 
                           Foreground="Gray" FontWeight="SemiBold"/>

                                    <TextBlock Text="{Binding Name}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="LightGray"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsModified}" Value="True">
                                                        <Setter Property="Foreground" Value="LightGreen"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding HasModifiedChildren}" Value="True">
                                                        <Setter Property="Foreground" Value="Yellow"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Grid>
                <GridSplitter Grid.Row="1" Height="2" Background="#282828" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                <Grid Name="ResourceListView" Grid.Row="2">
                    <DataGrid ItemsSource="{Binding ErpResources}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      Background="#FF161616" 
                      RowBackground="#FF1C1C1C"
                      Foreground="White"
                      BorderThickness="0"
                      GridLinesVisibility="None"
                      RowHeaderWidth="0"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      VerticalContentAlignment="Center"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridCell">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Height" Value="32"/>
                                <Setter Property="Background" Value="#FF161616"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type DataGridCell}">
                                            <Border Padding="5,0,5,0" Background="{TemplateBinding Background}" BorderThickness="0">
                                                <ContentPresenter VerticalAlignment="Center" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#FF3E3E40"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>

                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#FF282828"/>
                                <Setter Property="Foreground" Value="#FFCCCCCC"/>
                                <Setter Property="Height" Value="30"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="10,0,0,0"/>
                                <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                <Setter Property="BorderBrush" Value="#FF161616"/>
                            </Style>

                            <Style TargetType="DataGridRow">
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF2A2A2B"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Resource Name" Binding="{Binding Name}" Width="2*" />
                            <DataGridTextColumn Header="Type" Binding="{Binding ResourceType}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
            <GridSplitter Grid.Column="1" Width="2" Background="#282828" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <Grid Name="ContentArea" Grid.Column="2">
                <ContentControl Content="{Binding SelectedDocument}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>