<UserControl x:Class="AlterEgoEditor.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AlterEgoEditor.Views"
             mc:Ignorable="d">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2"/>
            <RowDefinition Height="250"/>
        </Grid.RowDefinitions>

        <Grid Name="TopSeparator" Grid.Row="0" Background="#282828"/>

        <Grid Name="StartMenuTopHalf" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Grid Name="LeftPanel" Grid.Column="0">
                <StackPanel Orientation="Vertical" VerticalAlignment="Top" Margin="20">
                    <Button Command="{Binding CreateNewProjectCommand}" Style="{StaticResource StartMenuRoundedHoverButtonStyle}" HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE710;" FontFamily="Segoe Fluent Icons" FontSize="18" Margin="0,0,15,0" VerticalAlignment="Center"/>
                            <TextBlock Text="New mod project" FontSize="16" FontWeight="Medium" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding OpenProjectFromStartCommand}" Style="{StaticResource StartMenuRoundedHoverButtonStyle}" HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE838;" FontFamily="Segoe Fluent Icons" FontSize="18" Margin="0,0,15,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Open mod project" FontSize="16" FontWeight="Medium" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <GridSplitter Grid.Column="1" Width="2" Background="#282828" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        </Grid>

        <GridSplitter Grid.Row="2" Height="2" Background="#282828" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

        <Grid Grid.Row="3" Margin="10,20,10,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Margin="10,0,10,0">
                <Button Command="{Binding OpenLinkCommand}" CommandParameter="https://discord.gg/R5dEjWXN" Style="{StaticResource StartMenuLinkButtonsStyle}" Background="#FF121212">
                    <DockPanel VerticalAlignment="Top">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
                            <TextBlock Text="AlterEgo Discord" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <TextBlock Text="Join the AlterEgo Editor Discord" FontSize="16" FontWeight="Light" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="" FontFamily="Segoe Fluent Icons" FontSize="18" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                    </DockPanel>
                </Button>
            </Grid>
            <Grid Grid.Column="1" Margin="10,0,10,0">
                <Button Command="{Binding OpenLinkCommand}" CommandParameter="https://www.nexusmods.com/games/f125" Style="{StaticResource StartMenuLinkButtonsStyle}" Background="#FF121212">
                    <DockPanel VerticalAlignment="Top">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
                            <TextBlock Text="NexusMods" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <TextBlock Text="Visit Nexus Mods to upload your mods" FontSize="16" FontWeight="Light" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="" FontFamily="Segoe Fluent Icons" FontSize="18" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                    </DockPanel>
                </Button>
            </Grid>
            <Grid Grid.Column="2" Margin="10,0,10,0">
                <Button Command="{Binding OpenLinkCommand}" CommandParameter="https://www.overtake.gg/downloads/categories/f1-series.216/" Style="{StaticResource StartMenuLinkButtonsStyle}" Background="#FF121212">
                    <DockPanel VerticalAlignment="Top">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
                            <TextBlock Text="Overtake.gg" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <TextBlock Text="Visit Overtake.gg to upload your mods" FontSize="16" FontWeight="Light" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="" FontFamily="Segoe Fluent Icons" FontSize="18" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                    </DockPanel>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
